apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: name-micro-ingress
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/rewrite-target: /$2
  labels:
    name: name-micro-ingress
spec:
  rules:
  - host: {hostname}
    http:
      paths:
      - path: /name/api(/|$)(.*)
        pathType: Prefix
        backend:
          service:
            name: name-micro-service
            port: 
              number: 80
