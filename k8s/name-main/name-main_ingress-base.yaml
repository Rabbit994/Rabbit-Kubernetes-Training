apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: name-main-ingress
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/rewrite-target: /$2
  labels:
    name: name-main-ingress
spec:
  rules:
  - host: {hostname}
    http:
      paths:
      - path: /name(/|$)(.*)
        pathType: Prefix
        backend:
          service:
            name: name-main-service
            port: 
              number: 80
